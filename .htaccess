# Enable mod_rewrite
RewriteEngine On

# Redirect .php URLs to the clean version without .php
RewriteCond %{REQUEST_FILENAME} -f                    # Ensure the file exists
RewriteRule ^(.*)\.php$ /$1 [R=301,L]                  # Redirect .php to clean URL
